# 6. CardCRUD

**Goal**: Create/edit/delete cards with title, description, and a color picker.
**Est.**: 2h
**Dependencies**: 05

## Steps
- [x] Render cards within their column
- [x] Build "new card" form (title, description, color)
- [x] Build edit card modal/inline editor
- [x] Wire delete card action
- [x] Apply the selected color as a card accent/background

## Definition of Done
- [x] Cards can be created, edited, deleted, and colored — `CardList`/`CardItem`/`CardForm` wired to `window.taskApi.cards`, 8-color swatch picker
- [x] Changes persist after restart — same SQLite storage layer verified persistent in Intent 03

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: `src/state/useCards.ts` hook for CRUD scoped to a column. `src/components/CardColorPicker.tsx` (8-swatch palette), `src/components/CardForm.tsx` (shared create/edit form: title, description, color), `src/components/CardItem.tsx` (colored left-border card, click to edit inline, × to delete with confirm), `src/components/CardList.tsx` (renders cards + "+ Add card" toggle). Wired into `KanbanBoard.tsx` inside each column. `npm run build` succeeds.
- **Follow-ups**: Same sandbox GUI limitation — manual click-through recommended (add a card with each color, edit one, delete one, restart and confirm cards/colors persist). Drag-and-drop reordering is Intent 07, due dates are Intent 08.
