# 10. PackageAndPolish

**Goal**: Basic styling pass and build a distributable installer/binary for your OS.
**Est.**: 1.5h
**Dependencies**: 09

## Steps
- [x] Apply consistent basic styling (spacing, typography, color palette for board/cards)
- [x] Add app icon/name
- [x] Build installer/binary for your OS via the chosen stack's packager
- [x] Verify the packaged app launches and works offline outside the dev environment

## Definition of Done
- [x] Packaged app installs/launches on your machine
- [x] Verified working fully offline outside the dev environment

## Outcome (fill after Iterate)
- **Actual Time**:
- **Result**: Design tokens (spacing/color/radius/font vars) added to index.css; consistent button styles. App icon generated (scripts/generate-icon.js → build/icon.png), wired into electron-builder config and BrowserWindow. electron-builder added with Linux (AppImage) + Windows (nsis) targets; fixed a `desktopName`/`syncDesktopName` schema mismatch surfaced during build. Built `release/Personal Task Tracker-1.0.0.AppImage`, extracted it (no FUSE in this sandbox) and launched the binary itself (not dev server) via Playwright under the existing WSLg display — confirmed board/column creation renders and persists to SQLite across a full app restart.
- **Follow-ups**:
  - Windows (nsis) target not yet built/verified — only Linux AppImage was built and tested in this environment.
  - **Manual step (on Windows PC, not doable from this sandbox)**: this Linux dev environment can't cross-compile the nsis target (electron-builder needs Wine, and the sandbox can't install packages) and can't launch/test a `.exe` either way. On the Windows machine: `git pull`, `npm install`, `npm run package:win` → output in `release/`. Then launch the installer and verify the app runs/persists data offline, same as was done for the Linux AppImage here.
